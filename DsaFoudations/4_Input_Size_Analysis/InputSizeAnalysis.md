Input Size Analysis

Learn:

- Understanding constraints
- Predicting acceptable complexity

Examples:

```
n <= 10       → O(n!)
n <= 20       → O(2ⁿ)
n <= 1000     → O(n²)
n <= 100000   → O(n log n)
n >= 1000000  → O(n)
```

---
